# v0.0.12 — 抬头图/条码/页码恢复显示，课程表补全教室与周次

**构建日期：** 2026-09-21

## 修复

- **抬头图、条码、码下数字、页数提示一并消失**：分页脚本要把手机端的排版算法搬进测量 WebView 执行，此前是按名逐个搬函数，而算法内部调用的一个私有辅助函数没被一起搬过去。它在定时器回调里抛"未定义"错误——那已经跳出了脚本自己的错误处理，所以既不报错也不回传结果，预览于是永远停在测量阶段，这几样全都不出现。现在整套算法收进一个自包含函数，结构上不再可能"忘了带上谁"，并加了一条把注入函数抠出来独立执行的回归检查。
- **超过一页高度的题目被裁切**：整块挪页时，比一页还高的块会被页面容器直接裁掉，内容凭空少一截。现在一路向下拆到装得下为止（真实作业纸是 表格 > 表体 > 行 > 单元格 的层层嵌套，中间每层往往只有一个孩子，所以单子块也要继续下钻），并把同一页内同源的相邻片段并回同一个外壳，表格不会被拆成两张。
- **APK 之间无法区分版本**：出图流程生成的工程里 versionCode 恒为 1，任何两个 APK 在 Android 看来都是同一版本，装上新的会被视为覆盖甚至被拒，也分不清装到的到底是哪一次构建。现在构建时按运行次数注入 versionCode，测试构建的版本号同时带上构建号。

## 新增

- **课程表区分课程与实验**：理论课（含其作业）整行改用蓝色标注——左侧色条、类型徽标、标题同色；实验与值日仍为橙色，两类事情在颜色上一眼分开。
- **课程表显示教室**：教室单独成行，数据没给时留空而不是和节次挤在一起。
- **课程表显示周次**：优先用服务器给的周次字段；没有时按学期起始日推算（对齐到日历周，学期中间开课不会少算一周），连续区间显示为"第3-12周"，断开则列举。推不出时不显示周次，不编一个数。
- **日程数据导出**：开发者模式下作业纸页新增"日程JSON"按钮，导出学期对象与日程条目的原始字段，用于确认教室、周次这些字段服务器到底放在哪儿。

## 说明

- 分页落点仍受字体影响：用真实作业纸量到整份正文高 938 CSS px，而一页版心是 933.5px，只差 4.5px。电脑版用宋体排版时行高更大、断点自然更靠前，因此页数与断点在换字体的边际上仍可能与电脑版差一页——这与分页算法无关，取决于中文宋体是否内置。
- 周次能否显示取决于服务器是否给学期起止日期或周次字段；新增的"日程JSON"导出可以一次确认。

## 验证

- 新增 `pc-analysis/check-schedule-fields.js`：周次推算与文案 14 项断言（连续/断开/去重/非法值忽略/日历周对齐/学期前不给周次/超范围不给/无日期字段时留空）。
- `pc-analysis/check-paper-pipeline.js` 增至 58 项断言，含"注入脚本里的分页函数能被抠出来独立执行"这条，专门守住上次那类漏带辅助函数的错。
- 发布判定与版本注入：`pc-analysis/simulate-release-gate.js` 四种触发路径实跑；`scripts/ci-version.js` 测试/正式两种模式实跑（含非法入参拒绝）。
- 用电脑版真实导出的 JSON 跑完整链路：条码解出 0801000011740 与真实 PDF 逐字符一致；抬头图与条码的页面对位经真实浏览器量得，条码上边与标识上边齐平、右端与答题区右端齐平。
- `npx tsc --noEmit` 通过；`npx expo export --platform web` 全量打包并渲染 14 条路由通过。
